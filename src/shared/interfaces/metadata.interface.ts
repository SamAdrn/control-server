/**
 * Represents the metadata information an object needs
 */
export interface Metadata {
    name: string;
    namePlural: string;
    label: string;
    labelPlural: string;
    description: string;
    keyName: string;
    sortBy: string[];
}
